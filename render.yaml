services:
  - type: web
    name: cracked
    env: node
    buildCommand: cd client && npm install --legacy-peer-deps && npm run build && cd .. && npm install --legacy-peer-deps && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
