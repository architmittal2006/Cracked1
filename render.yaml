services:
  - type: web
    name: cracked
    env: node
    buildCommand: cd client && npm install --legacy-peer-deps && cd .. && npm install
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
